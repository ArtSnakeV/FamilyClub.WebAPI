"use client";

/**
 * MOCK / FUTURE — картка «Хто зараз працює від імені компанії».
 * Потрібен presence / team API. Закоментовано, щоб не показувати мок у UI.
 *
 * type MockMember = {
 *   name: string;
 *   online: boolean;
 *   role: string;
 *   lastSeen?: string;
 * };
 *
 * const MOCK_TEAM: MockMember[] = [
 *   { name: "Ink", online: true, role: "Контент менеджер" },
 *   { name: "Maria", online: true, role: "Модерує відгуки" },
 *   { name: "Alex", online: true, role: "Додає книги" },
 *   {
 *     name: "Sofia",
 *     online: false,
 *     role: "Менеджер підтримки",
 *     lastSeen: "В мережі 2 год. тому",
 *   },
 * ];
 *
 * export default function TeamPresenceCard() {
 *   return (
 *     <div className="rounded-[12px] bg-white px-5 py-5 shadow-[0_0_15px_rgba(0,0,0,0.12)] flex flex-col gap-4 h-full">
 *       ...
 *     </div>
 *   );
 * }
 */

export default function TeamPresenceCard() {
  return null;
}
