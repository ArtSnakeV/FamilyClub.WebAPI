"use client";

/**
 * MOCK / FUTURE — картка «Сповіщення».
 * Потрібен notification preferences API. Закоментовано з UI.
 *
 * import SettingsToggle from "@/app/(admin-site)/admin/platform-settings/components/SettingsToggle";
 * import type { NotificationPrefs } from "../utils/mySettingsStorage";
 *
 * type Props = {
 *   value: NotificationPrefs;
 *   onChange: (next: NotificationPrefs) => void;
 * };
 *
 * const ROWS: Array<{ key: keyof NotificationPrefs; label: string }> = [
 *   { key: "newOrders", label: "Нові замовлення" },
 *   { key: "newComplaints", label: "Нові скарги" },
 *   { key: "reviews", label: "Відгуки" },
 *   { key: "newspaper", label: "Газета / Нові пости" },
 *   { key: "email", label: "Email повідомлення" },
 * ];
 *
 * export default function NotificationsCard({ value, onChange }: Props) {
 *   return ( ... toggles ... );
 * }
 */

export default function NotificationsCard() {
  return null;
}
